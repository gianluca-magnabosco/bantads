import { ClienteConsultaListagem } from './cliente-consulta-listagem.model';

describe('ClienteConsultaListagem', () => {
  it('should create an instance', () => {
    expect(new ClienteConsultaListagem()).toBeTruthy();
  });
});
