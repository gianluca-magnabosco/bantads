import { ClienteAprovacao } from './cliente-aprovacao.model';

describe('ClienteAprovacao', () => {
  it('should create an instance', () => {
    expect(new ClienteAprovacao()).toBeTruthy();
  });
});
