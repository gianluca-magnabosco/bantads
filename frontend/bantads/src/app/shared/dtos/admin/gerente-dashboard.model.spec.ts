import { GerenteDashboard } from './gerente-dashboard.model';

describe('GerenteDashboard', () => {
  it('should create an instance', () => {
    expect(new GerenteDashboard()).toBeTruthy();
  });
});
